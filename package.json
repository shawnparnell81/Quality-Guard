{
  "name": "qms-guardian",
  "version": "0.1.0",
  "private": true,
  "description": "Tooling that spans both workspaces (public/ and server/). The app itself has no build step; this is only the type checker.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "cd server && node --env-file=.env --test"
  },
  "devDependencies": {
    "typescript": "5.7.3"
  }
}
