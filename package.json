{
  "name": "qms-guardian",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tooling that spans both workspaces (public/ and server/). The app has no build step; the whole codebase is ES modules. This is only the type checker.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "cd server && node --env-file=.env --test"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
